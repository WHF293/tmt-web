import { CSSProperties } from "react"

export const defaultTheme: string = '#b96dfc'
export const defaultIconSize: number = 18

export const lf_box_style = {
  display: 'flex',
  position: 'relative',
} as CSSProperties
